# 凡人修仙传-人界篇 · 版本说明 v1.1.1

> 发布日期：2026-07-23
> 性质：v1.1 补充升级 — 出身系统深度清理 + 内容池大幅扩展
> 核心主题：彻底剥离固定叙事文本，AI 拿到的是纯数值种子

---

## v1.1.1 新增变更

### 一、`roll_origin()` 彻底剥离固定叙事

v1.1 中 `roll_origin()` 虽已标注"AI 自由创造"，但实际仍返回硬编码的"描述"/"环境特征"文本。v1.1.1 将其完全剥离：

| 维度 | v1.1 | v1.1.1 |
|:-----|:-----|:-----|
| 家族背景 | 返回 `{"描述": "你是孤儿出身…"}` | 移除"描述"，仅返回 type + numerical effects |
| 出生地 | 返回 `{"环境特征": "宁静，灵气稀薄"}` + `"出生地特征"` 字段 | 移除全部叙事文本和"出生地特征"字段 |
| 特殊事件 | 返回 `{"描述": "你出生时天降异象…"}` | 移除"描述"，仅返回 type + numerical effects |
| 返回值结构 | 扁平 dict（出身数值 + 叙事文本混在一起） | 三层嵌套 `family/birthplace/special_event` 各含 `{type, effects}` + 向后兼容顶层 key |

**新返回值范例：**
```json
{
  "family":        {"type": "猎户之家",     "effects": {"体魄加成": 10, "物品": "短刀"}},
  "birthplace":    {"type": "妖兽山脉边缘", "effects": {"灵草概率": true}},
  "special_event": {"type": "捡到残破玉简", "effects": {"物品": "残缺功法/法术"}},
  "家族背景": "猎户之家",
  "出生地":   "妖兽山脉边缘",
  "特殊事件": ["捡到残破玉简", {"物品": "残缺功法/法术"}]
}
```

### 二、出身池大幅扩展

| 维度 | v1.1 | v1.1.1 | 新增内容（示例） |
|:-----|:----:|:------:|:-----|
| 家族背景 | 13 | **23** | 铁匠之家、镖师之家、戏班/卖艺人、马帮护卫、守墓人之后、盗墓者之家、灵植夫之家、逃难流民、山中隐士之后、炼尸/赶尸世家 |
| 出生地 | 11 | **19** | 湖畔渔村、古道驿站、灵矿矿区、废弃宗门遗址、沼泽毒瘴区、地下溶洞群、大漠绿洲、海外孤岛 |
| 特殊事件 | 14 | **29** | 血脉返祖觉醒、月圆之夜异变、误食灵果/灵草、被灵兽认主、梦中得仙师指点、坠崖得机缘、被魔物附身失败、被邪修标记、救下受伤修士、偷听到修士密谈、捡到残缺藏宝图、河水冲来宝物、挖出前人遗骸/遗物、天生亲近某种灵气、被误认为他人之子 |

### 三、`generate_physique_detail()` 清理

| 位置 | 问题 | 修复 |
|:-----|:-----|:-----|
| `rarities` 列表 | 第 4 元素为固定叙事文本（如"你感觉自己的XX比常人稍强"）— 死代码 | 移除第 4 元素，仅保留 `(名称, 权重, 倍率)` |
| `"特殊向"` direction | `special` 值为一整段硬编码描述 | 改为 `{"special": True}` 类型标记 |
| `costs` 列表 | 7 条平铺的叙事字符串（如"情绪波动大，心境易下降"） | 改为结构化 `{"type": "xxx", "effect": "xxx"}` |

### 四、`calc_world.py` 同步扩展

`_FAMILY_PARAMS`、`_AREA_PARAMS`、`_EVENT_PARAMS` 三个参数映射表各新增所有对应条目，每个条目含完整的 `plot_hooks`、`keywords`、`tensions`、`location_types` 等叙事参考种子。

### 五、`service/game-rules.md` 同步更新

- 第 532-546 行：更新 `roll_origin()` 结果引用，明确 3 层嵌套结构访问路径
- 第 540-546 行：新增 3 组扩展示例开场逻辑

### 六、无改动文件（已验证无需修改）

全量扫描 `scripts/calculators/` 下 8 个 `.py` 文件，确认以下模块设计正确，不存在固定叙事文本混入问题：
- `calc_battle.py` — 纯游戏机制参数
- `calc_beast.py` — 纯概率引擎
- `calc_crafting.py` — 品质标签为游戏机制符号
- `calc_cultivation.py` — 事件标签为简短类型标记（3-7字）
- `calc_utils.py` — 纯工具函数

### 统计

| 维度 | v1.1 | v1.1.1 |
|:-----|:----:|:------:|
| 家族背景种类 | 13 | **23** |
| 出生地种类 | 11 | **19** |
| 特殊事件种类 | 14 | **29** |
| 固定叙事文本残留 | 22 条（描述+环境特征） | **0** |
| 修改文件数 | — | **3**（calc_character / calc_world / game-rules） |

---

# 凡人修仙传-人界篇 · 版本说明 v1.1

> 发布日期：2026-07-23
> 性质：重大架构升级 — AI创作自由度最大化
> 核心主题：从"固定菜单式游戏"转型为"AI动态叙事引擎"

---

## 一、升级概述

v1.0 的游戏开局存在大量固定内容：固定宗门列表、固定丹药名称、固定秘境类型、固定开场模板流程……玩家从菜单中选选项，AI 只是"填空机"。

v1.1 **彻底翻转了这一设计哲学**：所有固定列表被标记为"参考方向"或彻底删除，游戏核心从"菜单驱动"变为"叙事驱动"。AI（GM）现在是真正的创造者——宗门名字、丹药效果、势力关系、开场路径，全部由 AI 根据角色出身、灵根、属性实时生成。

---

## 二、设计原则变更

| 原则 | v1.0 | v1.1 |
|:-----|:-----|:-----|
| 宗门/势力 | 固定 6 个正道宗门列表 | AI 根据出身+灵根+地图动态创造 |
| 丹药/法器 | 固定 8 种丹药名 + 属性列表 | 方向性参考，AI 创造具体丹方和效果 |
| 修仙路径 | 菜单选择（正道/邪修/散修 × 13条） | 叙事自然引出，玩家通过行动选择路径 |
| 事件/秘境 | 固定类型表 + 代表名称 | 方向参考，AI 自由创造具体内容 |
| 符箓/阵法 | 固定类型+代表阵法名 | 方向参考，AI 自由命名和设计效果 |
| 体质/出身 | 概率引擎抽取，描述较固定 | 数值种子保留，叙事完全由 AI 发挥 |
| 拍卖/灵田 | 固定入场费/固定等级对应 | 数值模糊化，AI 根据剧情动态设定 |

### 新增核心原则（§1 第 11 条）

```
AI自主创造：本系统不设任何固定菜单选项。
所有宗门、势力、功法、丹药、事件、NPC等，
均由AI根据角色背景和世界情境动态创造。
参考列表仅供"灵感种子"，不得向玩家展示为固定选项。
玩家的每一个选择应来源于叙事，而非菜单。
```

---

## 三、文件级变更清单

### 3.1 `scripts/calculators/calc_world.py` — 重大重构

**移除：**
- `_SECT_PARAMS` 固定 13 个宗门数据字典（约 180 行）
- `roll_sect_path()` 和 `get_sect_by_name()` 菜单式选择函数

**新增：**
- 修仙路径动态生成指南（注释），含 AI 生成规则：
  - 如何根据出身萃取线索
  - 灵根如何影响被什么势力看重
  - 叙事驱动 vs 菜单驱动的正确/错误示例
  - 自由发挥方向参考（正道/邪修/散修各方向）

**软化：**
- 文件头和函数文档字符串标注"种子/参考框架，AI可自由发挥"
- `_FAMILY_PARAMS` / `_AREA_PARAMS` / `_EVENT_PARAMS` 标注为"参考参数"

### 3.2 `scripts/calculators/` 所有 Python 模块 — 文档字符串升级

| 文件 | 改动 |
|:-----|:-----|
| `calc_battle.py` | +注释：场景/状态/元素可由 AI 自由扩展，传参即用 |
| `calc_cultivation.py` | +注释：突破失败/天劫/业力/心境标签为叙事种子 |
| `calc_crafting.py` | +注释：丹药/法器/符箓名称效果由 AI 创造 |
| `calc_beast.py` | +注释：灵兽种类/名称/特殊能力由 AI 动态创造 |
| `calc_character.py` | +注释：家族故事/出生地细节/体质命名由 AI 发挥 |

函数文档字符串升级（5 个函数）：
- `calc_breakthrough_failure()` — "返回标签，AI 据此自由创造叙事细节"
- `calc_tribulation_type()` — "AI 自由创造天劫的具体形态和表现"
- `calc_karma_event()` — "返回方向标签，AI 据此自由创造具体事件叙事"
- `calc_mind_change()` — "AI 自由叙事描述心境波动的表现"
- `calc_alchemy()` / `calc_forging()` — "返回品质标签，AI 据此创造具体名称和外观描述"

### 3.3 `scripts/calculators/__init__.py` + `scripts/calc.py`

**移除导出：** `roll_sect_path`、`get_sect_by_name`

### 3.4 `service/game-rules.md` — 全面软化（12 处）

| 位置 | 改动 |
|:-----|:-----|
| §1 核心原则 | 新增第 11 条「AI 自主创造」 |
| §2.5 出身系统 | 改为"AI 自由创造叙事" |
| §3.1 功法品质 | 固定分级 → "参考分级" |
| §3.2 功法类型 | 移除详细规则（血遁/燃魂/搜魂示例），改为方向性描述 |
| §3.3 法宝品质 | 移除固定命名（"上古遗留有残缺法则"等），改为"参考加成范围" |
| §5.1 炼丹经验 | 删除精确公式（升级×100/成功+20~50），改为 AI 动态确定 |
| §5.2 丹药表 | 固定 8 种丹药 → 5 个方向参考表（修为/恢复/辅助/突破/延寿） |
| §6.2 炼器材料 | 移除固定来源列，标注"AI 可创造" |
| §7.1 阵法类型 | 移除代表阵法名称（五行阵/诛仙剑阵/八卦阵等） |
| §7.2 阵法等级 | 移除境界固定对应（5 阶可困元婴等） |
| §8.1 符箓类型 | 移除使用次数列，加注"AI 创造具体名称" |
| §9.2 灵兽品质 | 加注"AI 可创造独特品质命名" |
| §10.1 宗门结构 | 标注"参考框架，AI 可调整" |
| §10.4 开局路径 | **整节删除** |
| §11.1 秘境类型 | 移除"收益"列，加注"AI 不必拘泥于此表" |
| §14.1-14.3 | 移除精确业力/心境数值（-30/+20 等） |
| §16.2 通缉 | 移除固定三级（黄/红/黑）分级 |
| §17.1 拍卖 | 移除固定入场费数值 |
| §19.1 灵田 | 移除"价值"列，加注"AI 可创造独特类型" |
| §24 开场模板 | **全段重写**：从固定菜单→「🍃 仙途伊始」叙事驱动规则 |

### 3.5 `SKILL.md` — 原则升级

- 新增强制规则第 4 条「AI 自主创造」
- 新增"叙事驱动，非菜单驱动"正反面示例
- 移除过于详细的六步创建流程、检查清单

---

## 四、保留不变的数值框架

以下为核心游戏机制（概率引擎必需），保持不变：

- 境界体系（修为需求/寿元/突破率）
- 概率公式（命中率 75%、暴击 5%、天劫触发 30% 等）
- 五行相克系数（×1.5 / ×0.7）
- 装备槽位限制
- 属性初始值基准
- 灵根加权抽取权重
- 突破失败分段概率（轻伤 40% / 重伤 25% / 修为尽失 20% / 走火入魔 10% / 死亡 5%）

---

## 五、设计哲学

```
v1.0:  Python 计算概率 → AI 填空式叙事 → 玩家从菜单选择
v1.1:  Python 计算概率 → AI 自由创造叙事 → 玩家从故事中选择行动
```

核心边界：
- **概率引擎**（Python）：保留精确数值 → 保证游戏系统可运转
- **叙事层**（AI/GM）：全部释放 → 保证每次开局独一无二

---

## 六、统计

| 维度 | v1.0 | v1.1 |
|:-----|:----:|:----:|
| 概率函数 | 42 | **40**（移除 2 个宗门函数） |
| 规则模块 | 24 | 24（§10.4 删除，§24 重写） |
| 固定列表项 | ~80+ | **0**（全部转为方向参考） |
| 固定菜单页 | 3（宗门/邪修/散修） | **0** |
| 修改文件数 | — | **8** |
